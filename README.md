# Color_Detector
An algorithm using the webcam to detect a certain color in the frame
The algorithm takes each frame, converts it into HSV format. 
Then it recognizes the color in the frame and draws a bounding box around it. 
"limits" file defines the deviation and brightness limits of the desired color in HSV format.
